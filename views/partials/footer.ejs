                </div>
            </main>
        </div>
    </div>

    <!-- ======================= MODAL KONFIRMASI LOGOUT ======================= -->
    <div id="logoutModal" class="fixed inset-0 w-full h-full bg-black bg-opacity-75 flex justify-center items-center hidden z-50 p-4">
        <div class="bg-white rounded-lg p-8 max-w-md w-full text-center shadow-2xl">
            <div class="flex justify-center mb-6">
                <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center">
                    <svg class="w-8 h-8 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path></svg>
                </div>
            </div>
            <h2 class="text-2xl font-bold text-gray-800 mb-3">Konfirmasi Logout</h2>
            <p class="text-gray-600 mb-8">Apakah Anda yakin ingin keluar dari sesi ini?</p>
            <div class="flex justify-center space-x-4">
                <button onclick="closeLogoutModal()" class="w-full bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-2 px-6 rounded-lg transition-colors">
                    Batal
                </button>
                <a href="/auth/logout" class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded-lg transition-colors">
                    Ya, Logout
                </a>
            </div>
        </div>
    </div>
    <!-- ======================================================================= -->

    <script>
        const logoutModal = document.getElementById('logoutModal');

        function openLogoutModal() {
            if (logoutModal) {
                logoutModal.classList.remove('hidden');
            }
        }

        function closeLogoutModal() {
            if (logoutModal) {
                logoutModal.classList.add('hidden');
            }
        }
    </script>
</body>
</html>
